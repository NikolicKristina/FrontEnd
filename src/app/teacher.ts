import { UserTitle } from './userTitle';

export class Teacher {

  id: number;
  firstName: string;
  lastName: string;
  email: string;
  jmbg: string;
  address: string;
  title: UserTitle;
  dateOfBirth: any;
  grades: number[];
}
