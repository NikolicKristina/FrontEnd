import { UserTitle } from './userTitle';

export class Student {

  id: number;
  firstName: string;
  lastName: string;
  email: string;
  jmbg: string;
  address: string;
  title: UserTitle;
  dateOfBirth: any;
  parents: number[];
  schoolClass: number;
  grades: number[];
}
