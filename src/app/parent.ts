import { UserTitle } from './userTitle';

export class Parent {

  id: number;
  firstName: string;
  lastName: string;
  email: string;
  jmbg: string;
  address: string;
  title: UserTitle;
  dateOfBirth: any;
  students: number[];
}
